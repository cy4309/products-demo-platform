import Hero from "@/components/Hero";
import Sponsors from "@/components/Sponsors";

const Home: React.FC = () => {
  return (
    <div className="w-full h-full">
      <Hero />
      {/* snap-camera-kit-web */}
      {/* model-viewer-demo */}
      {/* product-showcase-r3f? */}
      {/* guandu-dock-r3f-mindar? */}
      <Sponsors />
    </div>
  );
};

export default Home;
